# OrangeHRM Automated Testing Project 🧪

**Course:** Software Verification & Validation Testing (SVVT) - Assignment II  
**Application Tested:** [OrangeHRM Open Source Demo](https://opensource-demo.orangehrmlive.com/)  
**Framework:** Selenium WebDriver (Python)

## 📌 Project Description
This repository contains the automated test suite and execution reports for the SVVT Individual Software Testing Project. The purpose of this project is to apply software testing concepts—including equivalence partitioning, decision table testing, and negative testing—on a real-world, enterprise-level Human Resource Management (HRM) application.

The test scenarios map directly to the functional and non-functional requirements defined in the primary assignment report, ensuring comprehensive traceability and code coverage for core modules such as Authentication, Employee Management (PIM), and Leave Management.

## 🛠️ Technology Stack
* **Language:** Python 3.x
* **Automation Framework:** Selenium WebDriver
* **Test Runner:** Pytest
* **Reporting:** Pytest-HTML

## 🚀 Setup & Installation

> **Note:** The virtual environment is not included in this repository (it is listed in `.gitignore`). You must create your own local environment by following the steps below.

1. **Clone the repository:**
   ```bash
   git clone https://github.com/AbellTT/OrangeHRM_Testing_SVVT.git
   cd OrangeHRM_Demo_Testing
   ```

2. **Create a virtual environment:**
   ```bash
   python -m venv venv
   ```

3. **Activate the virtual environment:**
   ```bash
   # On Windows:
   .\venv\Scripts\activate
   ```

4. **Install required dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Make sure Google Chrome is installed** (Selenium Manager will handle the ChromeDriver automatically).

## ▶️ Running the Tests

**Run the full test suite (all 7 automated tests):**
```bash
pytest tests/ --html=Final_Test_Report.html --self-contained-html
```

**Run only the Authentication suite:**
```bash
pytest tests/test_auth.py --html=tests/auth_test_report.html --self-contained-html
```

**Run only the PIM suite:**
```bash
pytest tests/test_pim.py --html=pim_test_report.html --self-contained-html
```

**Run a single specific test:**
```bash
pytest tests/test_auth.py::test_valid_login -v
```

## 📂 Repository Structure
```
OrangeHRM_Demo_Testing/
├── tests/
│   ├── conftest.py          # Pytest fixture (browser setup/teardown)
│   ├── test_auth.py         # TC_001–TC_004: Authentication tests
│   └── test_pim.py          # TC_005–TC_007: PIM / Employee Management tests
├── auth_test_report.html    # Auth suite execution report
├── pim_test_report.html     # PIM suite execution report
├── Final_Test_Report.html   # Combined full suite report
└── requirements.txt         # Required Python packages
```

## 🎓 Deliverables
This repository serves as the practical implementation (Task 5) of the SVVT Assignment II. The final test case execution logs and defect reports generated by this code are appended to the master project report submitted on April 23, 2026.
